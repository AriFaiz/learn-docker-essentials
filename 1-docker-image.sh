docker image ls //perintah untuk melihat image yang tersedia pada docker dekstop
docker images //perintah untuk melihat image yang tersedia pada docker dekstop
docker image pull redis:latest //perintah untuk mengambil image yang sudah ada pada docker registry, pada kasus ini saya menggunakan db redis (in memory) untuk tagsnya versi terbaru (latest)
docker image rm redis:lates //perintah untuk menghapus/remove image